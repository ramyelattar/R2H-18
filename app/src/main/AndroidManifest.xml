<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth LE (couple pairing + sync) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- WiFi Direct (fallback sync) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- Audio recording (voice safeword detection) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Notifications (daily dare) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration (haptic patterns) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Internet (Stripe payment ONLY — no other network calls) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Health Connect (heart rate from smartwatch) -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />

    <!-- Keep screen on during sessions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Do Not Disturb (mute notifications during sessions) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Phone state (pause session on incoming call) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".R2H18App"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.R2H18">

        <!-- Main activity (single activity, Compose host) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link for Stripe payment return -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="igniteai"
                    android:host="payment" />
            </intent-filter>
        </activity>

        <!-- Decoy activity alias: disguise as Calculator -->
        <activity-alias
            android:name=".MainActivityCalc"
            android:targetActivity=".MainActivity"
            android:label="@string/app_name_decoy_calc"
            android:icon="@android:drawable/sym_def_app_icon"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Decoy activity alias: disguise as Notes -->
        <activity-alias
            android:name=".MainActivityNotes"
            android:targetActivity=".MainActivity"
            android:label="@string/app_name_decoy_notes"
            android:icon="@android:drawable/sym_def_app_icon"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

    </application>
</manifest>
